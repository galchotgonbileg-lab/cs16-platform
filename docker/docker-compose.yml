version: "3.9"

services:
  cs16:
    container_name: cs16-server
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "27015:27015/udp"
      - "27015:27015/tcp"
    restart: unless-stopped